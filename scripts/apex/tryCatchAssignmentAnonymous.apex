try {
    integer A= 3;
    string B= 'test';
    sum integer = A+B;
    system.debug('sum');
} catch (Exception ex) {
    system.debug('You got it wrong, buddy!');
    if (exception) {
        integer b = 5;
        sum = a+b;
        System.debug('sum');
    }
}